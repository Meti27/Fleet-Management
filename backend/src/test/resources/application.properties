# Test-only overrides. The datasource is supplied by the Testcontainers
# @ServiceConnection; we just need a valid (>=32 byte) JWT secret so the
# context can start without external environment variables.
security.jwt.secret=test-secret-test-secret-test-secret-1234
security.jwt.exp-minutes=120
